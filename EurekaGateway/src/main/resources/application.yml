eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8080
spring:
  application:
    name: EurekaGateway
zuul:
  routes:
    api:
      path: /baiou/**
      serviceId: eurekaclient
      sensitiveHeaders: "*"